## poweradmin zones import

Import a DNS zone from a BIND zone file

### Synopsis

Import DNS records from a BIND-compatible zone file into Poweradmin.

```
poweradmin zones import [flags]
```

### Options

```
      --create-zone        Create zone if it does not exist
      --dry-run            Show what would be imported without making changes
      --file string        Path to BIND zone file (required)
  -h, --help               help for import
      --zone-name string   Target zone name (overrides $ORIGIN in file)
```

### Options inherited from parent commands

```
  -k, --api-key string   Poweradmin API key (overrides config and env)
  -u, --url string       Poweradmin URL (e.g. https://dns.example.com)
```

### SEE ALSO

* [poweradmin zones](poweradmin_zones.md)	 - Manage DNS zones

###### Auto generated by spf13/cobra on 21-Sep-2026
